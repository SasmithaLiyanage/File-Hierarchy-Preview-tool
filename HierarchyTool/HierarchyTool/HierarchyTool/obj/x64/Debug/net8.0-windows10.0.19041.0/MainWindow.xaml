<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HierarchyTool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HierarchyTool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="HierarchyTool">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid Padding="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition Width="1.8*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Editor / Templates -->
        <Border Grid.Column="0" Margin="0,0,12,0" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="6">
            <Grid Padding="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:ConnectionId='9' Content="Vite React" x:Name="ViteButton"                          />
                    <Button x:ConnectionId='10' Content="Spring Boot" x:Name="SpringButton"                            />
                    <Button x:ConnectionId='11' Content="Blank" x:Name="BlankButton"                           />
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,8" Spacing="8">
                    <Button x:ConnectionId='7' Content="Add File" x:Name="AddFileButton" />
                    <Button x:ConnectionId='8' Content="Add Folder" x:Name="AddFolderButton" />
                </StackPanel>

                <TextBox x:ConnectionId='6' x:Name="ScriptTextBox" Grid.Row="2" AcceptsReturn="True" TextWrapping="Wrap" PlaceholderText="Type file/folder script here..." />
            </Grid>
        </Border>

        <!-- Right: Preview and actions -->
        <Border Grid.Column="1" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="6">
            <Grid Padding="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:ConnectionId='5' x:Name="PreviewPanel" />
                </ScrollViewer>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,8,0,0">
                    <Button x:ConnectionId='2' Content="Export Markdown" x:Name="ExportMarkdownButton"                              />
                    <Button x:ConnectionId='3' Content="Export Image" x:Name="ExportImageButton"                           />
                    <Button x:ConnectionId='4' Content="Generate Files" x:Name="GenerateFilesButton"                             />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

